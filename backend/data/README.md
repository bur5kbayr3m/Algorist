# Data Storage Directory

This directory stores cached market data in JSON format.

Files created automatically:
- `market_data.json` - BIST100 stocks, Forex, Commodities (Group A)
- `funds_data.json` - TEFAS funds (Group B)

These files are gitignored and created on first run.
